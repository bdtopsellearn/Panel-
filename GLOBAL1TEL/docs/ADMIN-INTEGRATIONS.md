# Admin Integrations (No SMPP UI)
The Admin Panel now exposes HTTP Provider, API, Webhook and API Playground instead of the old Jasmin/SMPP configuration page.
Integration configuration is stored in the existing `system_settings` table.
Legacy SMPP backend files are retained for compatibility but are no longer linked from the Admin Panel.
